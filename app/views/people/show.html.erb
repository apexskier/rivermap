<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @person.first %>
</p>

<p>
	<b>Created: <%= @person.created_at.strftime("%B %d, %Y at %l:%M %p") %><br>
	<b>Updated: <%= @person.updated_at.strftime("%B %d, %Y at %l:%M %p") %>
</p>

<p>Currently logged in on <%= @person.sessions.size %> computer(s).</p>
